const Footer = () => {
    return (
        <footer className="border-top footer text-muted">
            <div className="container">
                &copy; React
            </div>
        </footer>
    );
}

export default Footer;